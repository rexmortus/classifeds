<% grouped_emoji.each do |emoji, count| %>
  <a href="#" class="label warning emoji reaction" data-emoji="<%= emoji %>"><%= emoji %> <%= count if count > 1 %></a>
<% end %>
<% if user_signed_in? %>
  <a class="label warning new-reaction hide-for-small" href="#">Add Reaction</a>
<% end %>
