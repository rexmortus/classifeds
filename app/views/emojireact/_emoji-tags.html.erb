<% grouped_emoji.each do |emoji, count| %>
  <a href="#" class="label warning reaction" data-emoji="<%= emoji %>"><%= emoji %> <%= count if count > 1 %></a>
<% end %>
<% if user_signed_in? %>
  <a class="label warning new-reaction" href="#">Add Reaction</a>
<% end %>
